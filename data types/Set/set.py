# set is collection of unordered,unchangeable and unidexable items
# set is mutable data type because we can add and remove items from set
# set elements are immutable because we cannot change item after creation
# but can add more items
# set is denoted by   {    }
# it remove duplicate elements
# we can't print by using index values


st={1,2,3,4,'a','b','c'}
print(st)

