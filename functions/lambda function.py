# lambda- it is a single line function.it have no name.
#it can have number of arguments and have only one ecpression
#  {
#     var=lambda arguments:expression
#  }
# ex: var(value)

test=lambda x:x*2
print(test(3))

test1=lambda y:y*y  #square of a no.
print(test1(4))

showname=lambda name:print('hi'+name)
showname('ramu')